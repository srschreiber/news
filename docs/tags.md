# Tags

Every briefing is tagged with its topic, period (`YYYY`, `YYYY-MM`), themes, and
the keywords extracted from its events. Browse by tag below.

<!-- The Material "tags" plugin renders the tag index here. -->
